# Python
venv/
__pycache__/
*.pyc
.env

# Vector store (regenerated by ingest.py — don't commit, can get large)
chroma_db/

# Your personal PDFs (don't commit unless you want them public)
pdfs/*
!pdfs/.gitkeep

# macOS
.DS_Store
